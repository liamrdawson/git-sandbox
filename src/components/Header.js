import React from 'react';

const Header = () => {
    return (
        <React.Fragment>
            <header>
                <h1>Git Sandbox</h1>
                <span>You can use this page to try out different Git techniques.</span>  
            </header>
        </React.Fragment>
    );
}

export default Header;